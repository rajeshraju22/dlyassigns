function calcirclearea(r)
{
    var area = (3.14 * r * r);
    document.write("r=" + r +"</br>");
    document.write("area=" + area);
}

function calcriclearea(r)
{
    var area2 = (11.2 * r * r);
    document.write("r=" + r);
    document.write("area" + r);
}
function calcriclearea(r)
{
    var area3 = (20.2 * r * r);
    document.write("r" + r + r);
    document.write("area" + r);
}